<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roszyk.maksymilian.biblioteka">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/landing_page_header" android:name=".biblioteka"/>
        <activity android:label="@string/bi_header" android:name=".DodawanieBiblioteki"/>
        <activity android:label="@string/bb_header" android:name=".PrzegladarkaBiblioteki"/>
        <activity android:label="@string/dk_header" android:name=".DodajKsiazke"/>
        <activity android:label="@string/df_header" android:name=".DodajFilm"/>
        <activity android:label="@string/pk_header" android:name=".PodgladKsiazki"/>
        <activity android:label="@string/pf_header" android:name=".PodgladFilmu"/>
    </application>

</manifest>